
public class Driver {
	
	public static void main(String[] args) {
		Student[] arr = new Student[10];
/*		
		Student joe = new Student("joe", "freshman", "A", "B");
		Student mary = new Student("mary", "sophomore", "A", "A");
		Student sue = new Student("sue", "freshman", "B", "B");
		
		String[] ctJoe = {"COSC 111", "COSC 211"};
		joe.setClasses
		
		String[] ctMary = {"COSC "
		
		arr[0] = joe;
		arr[1] = mary;
		arr[2] = sue;
		
		
		
		//find student with the highest GPA:
		
		double max = -1;
		Student bestStudent = null;
		for(int i=0; i<arr.length; i++) {
			if(arr[i] != ull && arr[i].getGpa() > max) {
				max = arr[i].getGpa();
				bestStudent = arr[i];
			}
			//System.out.println(max);
			System.out.println("best student is " +bestStudent.getName()+ " with a GPA of " +max);
		}
	*/	
	}

}
